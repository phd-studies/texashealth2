name: Deploy Frontend to Debian VM

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Build + Deploy on Azure VM
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          command_timeout: 20m
          script: |
            set -e
            cd ~/texashealth2

            git fetch origin main
            git reset --hard origin/main

            cd app/frontend

            # Ensure .env.local has AZURE_STORAGE_CONNECTION_STRING set on this VM
            npm install
            npm run build

            sudo env "PATH=$PATH" pm2 delete texashealth-ui || true
            cd ~/texashealth2/app/frontend
            sudo env "PATH=$PATH" PORT=80 pm2 start npm --name texashealth-ui -- run start
            sudo env "PATH=$PATH" pm2 save
            sudo env "PATH=$PATH" pm2 status
